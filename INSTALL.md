# Install

No Marketplace account needed. Download the latest `.vsix` from the [releases page](../../releases/latest), then in VS Code:

`Extensions` panel -> `...` menu -> `Install from VSIX...` -> select the file.

Or from a terminal: `code --install-extension <file>.vsix`
